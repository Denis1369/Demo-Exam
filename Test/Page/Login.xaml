<Window x:Class="Test.Page.Login"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Test.Page"
        mc:Ignorable="d" 
        Icon="/Data/Icon.ico"
        Title="Авторизация" Height="350" Width="400" ResizeMode="NoResize" WindowStartupLocation="CenterScreen">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Image Source="/Data/Icon.ico" Height="70" Width="70" Grid.Row="0" Margin="0,0,0,10"/>

        <Grid Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="200"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Label Content="Логин:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Margin="5"/>
            <TextBox x:Name="LoginText" Grid.Row="0" Grid.Column="1" 
                     FontSize="16" Margin="5" 
                     TextWrapping="NoWrap" VerticalContentAlignment="Center"/>

            <Label Content="Пароль:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Margin="5"/>
            <TextBox x:Name="PassText" Grid.Row="1" Grid.Column="1" 
                         FontSize="16" Margin="5" 
                         VerticalContentAlignment="Center"/>
        </Grid>

        <StackPanel Grid.Row="2" HorizontalAlignment="Center" Width="200">
            <Button x:Name="LoginButtton" Content="Войти" 
                    Margin="0,5" Padding="5"
                    Click="LoginButtton_Click" Background="MediumSpringGreen"/>

            <Button x:Name="GeustButton" Content="Войти как гость" 
                    Margin="0,5" Padding="5"
                    Click="GeustButton_Click" Background="Chartreuse"/>
        </StackPanel>

    </Grid>
</Window>